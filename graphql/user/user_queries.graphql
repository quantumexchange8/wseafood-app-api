type User {
    id: ID!
    full_name: String!
    email: String!
    dial_code: String!
    phone: String!
    dob: String!
    point: String!
}

type Query {
    me: User! @guard @auth
}

type UserResponse {
    success: Boolean!
    message: [String!]!
    token: String
    user: User
}
